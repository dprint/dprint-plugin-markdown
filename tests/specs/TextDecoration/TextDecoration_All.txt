!! should format !!
test *emphasis* or _emphasis_ test

test **strong** or __strong__ test

Both **strong and _emphasis_ and *emphasis***.

Test ~~strikethrough~~ test

[expect]
test _emphasis_ or _emphasis_ test

test **strong** or **strong** test

Both **strong and _emphasis_ and _emphasis_**.

Test ~~strikethrough~~ test

!! should leave as-is when asterisk is in middle of word for GitHub ~~

*wor*d
*word*1
*word*?

[expect]
*wor*d
*word*1
_word_?

!! should not separate adjacent text decoration !!
**a**_b_

[expect]
**a**_b_

!! should turn a tab between adjacent nodes into a space !!
**a**	_b_

[expect]
**a** _b_

!! should not let a decoration's delimiter run into the one it wraps !!
*_foo_*

_*foo*_

[expect]
*_foo_*

*_foo_*

!! should not let a decoration's delimiter run into its text !!
foo *_*

foo __*__

[expect]
foo *_*

foo __*__

!! should not let text within a decoration pair with its delimiter !!
*foo __bar *baz bim__ bam*

[expect]
_foo __bar *baz bim__ bam_

!! should keep asterisks for a decoration within a word !!
foo*bar*

foo***bar***baz

[expect]
foo*bar*

foo***bar***baz

!! should leave an escaped delimiter out of the reckoning !!
foo **\***

foo _\__

[expect]
foo **\***

foo _\__
