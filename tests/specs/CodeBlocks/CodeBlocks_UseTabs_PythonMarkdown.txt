~~ useTabs: true, list.indentKind: pythonMarkdown ~~
!! should write the tab after the spaces a list indents the code block by !!
- one

      code

[expect]
- one

    	code

!! should write the tab after the spaces a definition indents the code block by !!
term

: one

      code

[expect]
term

: one

    	code

!! should not write a tab where a definition's marker is written on the line !!
term
:       code

[expect]
term
: code

!! should write the tab after the spaces two lists indent the code block by !!
- - one

          code

[expect]
- - one

        	code

!! should write the tab after the spaces an ordered list indents the code block by !!
1. one

       code

[expect]
1. one

    	code

!! should write the tab after the spaces a list within a footnote definition indents by !!
[^1]: - one

          code

[expect]
[^1]: - one

        	code

!! should keep the spaces of a code block within a block quote within a list !!
- > one
  >
  >     code

[expect]
- > one
    >
    >     code
