~~ useTabs: true ~~
!! should indent the body of a footnote definition with a tab !!
[^1]: One.

    Two.

[expect]
[^1]: One.

	Two.

!! should write the tab before the markers of a block quote within a footnote !!
[^1]: One.

    > quote
    > more

[expect]
[^1]: One.

	> quote
	> more

!! should keep the spaces a list within a footnote indents by !!
[^1]: One.

    - a
    - b
      - c

[expect]
[^1]: One.

	- a
	- b
	  - c

!! should write a tab for each of a footnote and the code block within it !!
[^1]: One.

        code

[expect]
[^1]: One.

		code

!! should leave the indentation an html block keeps for itself alone !!
[^1]: One.

      <div>
      </div>

[expect]
[^1]: One.

	  <div>
      </div>

!! should keep the spaces of a footnote within a block quote !!
> [^1]: One.
>
>     Two.

[expect]
> [^1]: One.
>
>     Two.

!! should keep the spaces of a footnote a list indents off a tab stop !!
- [^1]: One.

      Two.

[expect]
- [^1]: One.

      Two.

!! should write the tab after the spaces a list indents the footnote by !!
- - [^1]: One.

        Two.

[expect]
- - [^1]: One.

    	Two.

!! should keep the spaces of a footnote definition written within another !!
[^a]: outer

    [^b]: inner

        - one

[expect]
[^a]: outer

	[^b]: inner

	    - one

!! should write the tab before the markers of a block quote a list holds !!
[^a]: note

    - item

      > quote
      > more

[expect]
[^a]: note

	- item

	  > quote
	  > more

!! should not write a tab where the label is written on the line !!
text[^a]

[^a]:

        code

[expect]
text[^a]

[^a]: code

!! should write the tab on each line of a fenced code block !!
[^1]: One.

    ```js
    code
    ```

[expect]
[^1]: One.

	```js
	code
	```

!! should write the tab on each row of a table !!
[^1]: One.

    | a | b |
    | - | - |
    | 1 | 2 |

[expect]
[^1]: One.

	| a | b |
	| - | - |
	| 1 | 2 |
